version: "3.9"

services:
  api:
    build: .
    depends_on:
      db:
        condition: service_healthy
    env_file:
      - .env
    ports:
      - "3000:3000"
    # Apply migrations, then start the server

volumes:
  pgdata:
